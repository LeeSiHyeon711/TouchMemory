# TouchMemory 환경변수 초안 (FEAT-01 — 최종 통합은 FEAT-06에서)
# 이 파일은 Python에서 파싱하지 않는다. run.sh가 export하여 os.environ으로 전달한다(FEAT-06).

# SQLite DB 파일 경로
DB_PATH=data/touchmemory.db

# 타임존
TZ=Asia/Seoul

# 중앙 API 서버 베이스 URL (FEAT-02에서 사용)
API_BASE_URL=http://127.0.0.1:8787
